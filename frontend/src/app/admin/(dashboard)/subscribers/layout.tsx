import type { Metadata } from "next";

export const metadata: Metadata = {
  title: {
    absolute: "Admin | Kayıtlı E-postalar | NEWU",
  },
};

export default function SubscribersLayout({ children }: { children: React.ReactNode }) {
  return children;
}
